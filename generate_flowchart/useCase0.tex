\documentclass[convert={density=300,size=1080x800,outext=.png}]{standalone}
\input{preamble.tex}
\begin{document}
\pagestyle{empty}
\input{macros.tex}

% User case 0: No containers + no CWL
% trimmomatic -> Fastqc -> hisat2 (bash script)
\begin{tikzpicture}[node distance = 2cm, auto]
  \centering
    % Place nodes
    \node [noContainer] (trimmomatic) {trimmomatic};
    \node [noContainer, below of=trimmomatic] (rightFastqc) {Fastqc};
    \node [noContainer, below of=rightFastqc] (hisat2) {hisat2};
    \node [method, below of=hisat2] (bash) {bash script};
    % Draw edges
    \path [arrow] (trimmomatic) -- (rightFastqc);
    \path [arrow] (rightFastqc) -- (hisat2);
    \path [line, dashed] (hisat2)--(bash);
\end{tikzpicture}

\end{document}
